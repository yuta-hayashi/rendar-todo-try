<p style="color: green"><%= notice %></p>

<h1>Tasks</h1>
<%= link_to "New task", new_task_path %>

<div id="tasks">
  <% @tasks.each do |task| %>
    <%= link_to(task_path(task)) do %>
      <%= render task %>
    <% end %>
  <% end %>
</div>
